import type { ExamExperience } from '../types';

/**
 * Candidate testimony is intentionally empty until each item has a retrievable
 * source URL and explicit publication decision. Previous illustrative entries
 * were moved to the external quarantine and are not presented as real recalls.
 */
export const examExperiences: ExamExperience[] = [];
